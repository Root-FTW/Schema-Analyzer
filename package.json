{
  "name": "schema-analyzer-root",
  "version": "1.0.0",
  "description": "Schema.org Analyzer with Express backend and React frontend",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "build": "cd schema-analyzer && npm install --legacy-peer-deps && npm run build",
    "dev": "concurrently \"node api/index.js\" \"cd schema-analyzer && npm run dev\""
  },
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
