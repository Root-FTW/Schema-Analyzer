{
  "name": "schema-analyzer-root",
  "version": "1.0.0",
  "description": "Schema.org Analyzer with Express backend and React frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd schema-analyzer && npm install && npm run build",
    "dev": "concurrently \"node server.js\" \"cd schema-analyzer && npm run dev\""
  },
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
